The bug was caused by the incorrect calculation of the `len` variable using `Character.codePointCount(input, 0, input.length())`, which misinterpreted the input string's length when surrogate pairs were present, leading to improper handling of code points. This resulted in the method failing to correctly process strings containing surrogate pairs, causing skipped or mishandled characters and producing unexpected or incomplete output. Evidence for this issue comes from the failing unit test `testLang720`, which involved a string with a surrogate pair (`"\ud842\udfb7"`) followed by a regular character (`"A"`) and verified that the escaped output should match the input. The bug was resolved by initializing `len` to `input.length()` and updating the loop logic to increment `pos` based on the length of processed characters (`c.length`), ensuring proper handling of surrogate pairs without unnecessary adjustments.