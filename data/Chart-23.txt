The bug was caused by the absence of a custom `equals` method in the `MinMaxCategoryRenderer` class, resulting in the default `Object` implementation being used, which only checks for reference equality instead of comparing the object's internal state. This led to instances of `MinMaxCategoryRenderer` being incorrectly considered unequal even when their `plotLines`, `groupPaint`, and `groupStroke` fields were identical, potentially causing issues in caching, collections, or unit tests. Evidence for this comes from the `testEquals` unit test, which failed when comparing two instances with identical states but different references, highlighting the need for a proper equality check. The issue was resolved by implementing a custom `equals` method that compares the relevant fields and delegates to the superclass's `equals` method, ensuring equality is based on the object's state.