The bug was caused by a missing condition in the `while` loop, which failed to account for functions with variable-length arguments (`var_args`) after all regular parameters were processed. This led to incorrect type-checking behavior, resulting in missed type mismatches or erroneous type errors for such functions. Evidence of the issue is demonstrated in the test case `testFunctionArguments16`, where a function `g` with `var_args` fails to report a type mismatch for its arguments (`1` and `true`). The bug was resolved by updating the `while` loop condition to include a check for `parameter != null && parameter.isVarArgs()`, ensuring proper handling of variable-length arguments during type-checking.