The bug was caused by the use of an incorrect constructor, `Partial(iChronology, newTypes, newValues)`, which bypassed validation, instead of the correct constructor, `Partial(newTypes, newValues, iChronology)`, that ensures proper validation of field types and values. As a result, the `Partial` instance created in the `with` method could contain invalid or inconsistent data, leading to runtime errors or incorrect behavior, such as failing to match against a `ReadableInstant` or throwing unexpected exceptions. Evidence for this issue is found in the unit test `testWith3`, which expects an `IllegalArgumentException` for invalid field addition but fails due to the skipped validation. The bug was resolved by replacing the incorrect constructor call with the correct one, ensuring full validation and preserving the integrity of the `Partial` instance.